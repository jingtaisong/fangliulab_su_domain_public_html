<?php	
	$up = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up->autoThumb = false; //可省略 
    $up->thumbWidth = 40; //可省略
    $up->thumbHeight = 40; //可省略
	$up->upload('goods_img');
	$goods_img=$up->imgPath;
	
	$up = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up->autoThumb = false; //可省略 
    $up->thumbWidth = 400; //可省略
    $up->thumbHeight = 300; //可省略
	$up->upload('upfile1');
	$upfile1=$up->imgPath;
	
	$upq1 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq1->autoThumb = false; //可省略 
    $upq1->thumbWidth = 40; //可省略
    $upq1->thumbHeight = 40; //可省略
	$upq1->upload('upfile2');
	$upfile2=$upq1->imgPath;
	
	$upq3 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq3->autoThumb = false; //可省略 
    $upq3->thumbWidth = 40; //可省略
    $upq3->thumbHeight = 40; //可省略
	$upq3->upload('upfile3');
	$upfile3=$upq3->imgPath;
	
	$upq4 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq4->autoThumb = false; //可省略 
    $upq4->thumbWidth = 40; //可省略
    $upq4->thumbHeight = 40; //可省略
	$upq4->upload('upfile4');
	$upfile4=$upq4->imgPath;
	
	$upq5 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq5->autoThumb = false; //可省略 
    $upq5->thumbWidth = 40; //可省略
    $upq5->thumbHeight = 40; //可省略
	$upq5->upload('upfile5');
	$upfile5=$upq5->imgPath;
	
	$upq6 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq6->autoThumb = false; //可省略 
    $upq6->thumbWidth = 40; //可省略
    $upq6->thumbHeight = 40; //可省略
	$upq6->upload('upfile6');
	$upfile6=$upq6->imgPath;
	
	$upq7 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq7->autoThumb = false; //可省略 
    $upq7->thumbWidth = 40; //可省略
    $upq7->thumbHeight = 40; //可省略
	$upq7->upload('upfile7');
	$upfile7=$upq7->imgPath;
	
	$upq8 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq8->autoThumb = false; //可省略 
    $upq8->thumbWidth = 40; //可省略
    $upq8->thumbHeight = 40; //可省略
	$upq8->upload('upfile8');
	$upfile8=$upq8->imgPath;
	
	$upq9 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq9->autoThumb = false; //可省略 
    $upq9->thumbWidth = 40; //可省略
    $upq9->thumbHeight = 40; //可省略
	$upq9->upload('upfile9');
	$upfile9=$upq9->imgPath;
	
	$upq10 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq10->autoThumb = false; //可省略 
    $upq10->thumbWidth = 40; //可省略
    $upq10->thumbHeight = 40; //可省略
	$upq10->upload('upfile10');
	$upfile10=$upq10->imgPath;
	
	$upq11 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $upq11->autoThumb = false; //可省略 
    $upq11->thumbWidth = 40; //可省略
    $upq11->thumbHeight = 40; //可省略
	$upq11->upload('upfile11');
	$upfile11=$upq11->imgPath;
	 // HTML中<input />的name属性值
	

	
    $up1 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up1->autoThumb = false; //可省略 
    $up1->thumbWidth = 40; //可省略
    $up1->thumbHeight = 40; //可省略
	$up1->upload('goods_img');// HTML中<input />的name属性值
	$goods_img1=$up1->imgPath;
	
	$up2 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up2->autoThumb = false; //可省略 
    $up2->thumbWidth = 40; //可省略
    $up2->thumbHeight = 40; //可省略
	$up2->upload('upfil2');
	$upfil2=$up2->imgPath;
	
	$up3 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up3->autoThumb = false; //可省略 
    $up3->thumbWidth = 40; //可省略
    $up3->thumbHeight = 40; //可省略
	$up3->upload('upfil3');
	$upfil3=$up3->imgPath;
	
	$up4 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up4->autoThumb = false; //可省略 
    $up4->thumbWidth = 40; //可省略
    $up4->thumbHeight = 40; //可省略
	$up4->upload('upfil4');
	$upfil4=$up4->imgPath;
	
	$up5 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up5->autoThumb = false; //可省略 
    $up5->thumbWidth = 40; //可省略
    $up5->thumbHeight = 40; //可省略
	$up5->upload('upfil5');
	$upfil5=$up5->imgPath;
	
	$up6 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up6->autoThumb = false; //可省略 
    $up6->thumbWidth = 40; //可省略
    $up6->thumbHeight = 40; //可省略
	$up6->upload('upfil6');
	$upfil6=$up6->imgPath;
	
	$up7 = new upimg_class("../upload","../upload/thumb"); //：$up = new upimg();
    $up7->autoThumb = false; //可省略 
    $up7->thumbWidth = 40; //可省略
    $up7->thumbHeight = 40; //可省略
	$up7->upload('upfil7');
	$upfil7=$up7->imgPath;

class upimg_class{
    public $uploadFolder = '../upload'; // 图片存放目录
    public $thumbFolder = '../upload/thumb'; // 缩略图存放目录
    public $thumbWidth = ''; // 缩略图宽度
    public $thumbHeight = ''; // 缩略图高度
    public $autoThumb = ''; // 是否自动生成缩略图
    public $error = ''; // 错误信息
    public $imgPath = ''; // 上传成功后的图片位置
    public $thumbPath = ''; // 上传成功后的缩略图位置

    // 说明：初始化，创建存放目录
    function __construct($uploadFolder = '../upload', $thumbFolder = '../upload/thumb'){
        $this->uploadFolder = $uploadFolder;
        $this->thumbFolder = $thumbFolder;
        $this->_mkdir();
    }

    // 说明：上传图片，参数是<input />的name属性值；成功返回图片的相对URL，失败返回FALSE和错误信息（在$this->error里）
    // bool/sting upload(string $html_tags_input_attrib_name);
    function upload($inputName){ // 上传操作，参数是input标签的name属性。
        if ($this->error){ // 如果有错，直接返回（例如_mkdir）
            return FALSE;
        }
        if(!$_FILES[$inputName]["name"]){
            $this->error = '没有上传图片';
            return FALSE;
        }
        if($_FILES[$inputName]["name"]){
            $isUpFile = $_FILES[$inputName]['tmp_name'];
            if (is_uploaded_file($isUpFile)){
                $imgInfo = $this->_getinfo($isUpFile);
                if (FALSE == $imgInfo){
                    return FALSE;
                }
                $extName = $imgInfo['type'];
                $microSenond = floor(microtime()*10000);// 取一个毫秒级数字,4位。
                $newFileName = $uploadFolder . '/' . date('YmdHis') . $microSenond . '.' . $extName ; // 所上传图片的新名字。
                $location = $this->uploadFolder . $newFileName;
                $result = move_uploaded_file($isUpFile, $location);
                if ($result){
                    if (TRUE == $this->autoThumb){ // 是否生成缩略图
                        $thumb = $this->thumb($location, $this->thumbWidth, $this->thumbHeight);
                        if (FALSE == $thumb){
                            return FALSE;
                        }
                    }
                    $this->imgPath = $location;
                    return $location;
                }else{
                    $this->error = '移动临时文件时出错';
                    return FALSE;
                }
            }else{
                $uploadError = $_FILES[$inputName]['error'];
                if (1 == $uploadError){ // 文件大小超过了php.ini中的upload_max_filesize
                    $this->error = '文件太大，服务器拒绝接收大于' . ini_get('upload_max_filesize') . '的文件';
                    return FALSE;
                }elseif (3 == $uploadError){ // 上传了部分文件
                    $this->error = '上传中断，请重试';
                    return FALSE;
                }elseif (4 == $uploadError){
                    $this->error = '没有文件被上传';
                    return FALSE;
                }elseif (6 == $uploadError){
                    $this->error = '找不到临时文件夹，请联系您的服务器管理员';
                    return FALSE;
                }elseif (7 == $uploadError){
                    $this->error = '文件写入失败，请联系您的服务器管理员';
                    return FALSE;
                }else{
                    if (0 != $uploadError){
                        $this->error = '未知上传错误，请联系您的服务器管理员';
                        return FALSE;
                    }
                } // end if $uploadError
            } // end if is_uploaded_file else
        } // end if $_FILES[$inputName]["name"]
    }

    // 说明：获取图片信息，参数是上传后的临时文件，成功返回数组，失败返回FALSE和错误信息
    // array/bool _getinfo(string $upload_tmp_file)
    private function _getinfo($img){
        if (!file_exists($img)){
            $this->error = '找不到图片，无法获取其信息';
            return FALSE;
        }
        $tempFile = @fopen($img, "rb");
        $bin = @fread($tempFile, 2); //只读2字节
        @fclose($tempFile);
        $strInfo = @unpack("C2chars", $bin);
        $typeCode = intval($strInfo['chars1'] . $strInfo['chars2']);
        $fileType = '';
        switch ($typeCode){ // 6677:bmp 255216:jpg 7173:gif 13780:png 7790:exe 8297:rar 8075:zip tar:109121 7z:55122 gz 31139
            case '255216':
                $fileType = 'jpg';
                break;
            case '7173':
                $fileType = 'gif';
                break;
            case '13780':
                $fileType = 'png';
                break;
            default:
                $fileType = 'unknown';
        }
        if ($fileType == 'png' || $fileType == 'gif' || $fileType == 'jpg'){
            $imageInfo = getimagesize($img);
            $imgInfo['size'] = $imageInfo['bits'];
            $imgInfo["type"] = $fileType;
            $imgInfo["width"] = $imageInfo[0];
            $imgInfo["height"] = $imageInfo[1];
            return $imgInfo;
        }else{ // 非图片类文件信息
            $this->error = '图片类型错误';
            return FALSE;
        }
    } // end _getinfo

    // 说明：生成缩略图，等比例缩放或拉伸
    // bool/string thumb(string $uploaded_file, int $thumbWidth, int $thumbHeight, string $thumbTail);
    function thumb($img, $thumbWidth = 400, $thumbHeight = 300, $thumbTail = '_thumb'){
        $filename = $img; // 保留一个名字供新的缩略图名字使用
        $imgInfo = $this->_getinfo($img);
        if (FALSE == $imgInfo){
            return FALSE;
        }
        $imgType = $imgInfo['type'];
        switch ($imgType){ // 创建一个图，并给出扩展名
            case 'gif' :
                $img = imagecreatefromgif($img);
                $extName = 'gif';
                break;
            case 'png' :
                $img = imagecreatefrompng($img);
                $extName = 'png';
                break;
            case "jpg" :
                $img = imagecreatefromjpeg($img);
                $extName = 'jpg';
                break;
            default : // 如果类型错误，生成一张空白图
                $img = imagecreate($thumbWidth,$thumbHeight);
                imagecolorallocate($img,0x00,0x00,0x00);
                $extName = 'jpg';
        }
        // 缩放后的图片尺寸(小则拉伸，大就缩放)
        $imgWidth = $imgInfo['width'];
        $imgHeight = $imgInfo['height'];
        if ($imgHeight > $imgWidth){ // 竖图
            $newHeight = $thumbHeight;
            $newWidth = ceil($imgWidth / ($imgHeight / $thumbHeight ));
        }else if($imgHeight < $imgWidth){ // 横图
            $newHeight = ceil($imgHeight / ($imgWidth / $thumbWidth ));
            $newWidth = $thumbWidth;
        }else if($imgHeight == $imgWidth){ // 等比例图
            $newHeight = $thumbWidth;
            $newWidth = $thumbWidth;
        } 
//注释的是等比例的

        $bgimg = imagecreatetruecolor($newWidth,$newHeight);
        $bg = imagecolorallocate($bgimg,0x00,0x00,0x00);
        imagefill($bgimg,0,0,$bg);
        $sampled = imagecopyresampled($bgimg,$img,0,0,0,0,$newWidth,$newHeight,$imgWidth,$imgHeight);
        if(!$sampled ){
            $this->error = '缩略图生成失败';
            @unlink($this->uploadFolder . '/' . $filename); // 删除上传的图片
            return FALSE;
        }
        $filename = basename($filename);
        $newFileName = substr($filename, 0, strrpos($filename, ".")) . $thumbTail . '.' . $extName ; // 新名字
        $thumbPath = $this->thumbFolder . '/' . $newFileName;
        switch ($extName){
            case 'gif':
                $result = imagegif($bgimg, $thumbPath);
                break;
            case 'png':
                $result = imagepng($bgimg, $thumbPath);
                break;
            case 'jpg':
                $result = imagejpeg($bgimg, $thumbPath);
                break;
            default: // 上边判断类型出错时会创建一张空白图，并给出扩展名为jpg
                $result = imagejpeg($bgimg, $thumbPath);
        }
        if ($result){
            $this->thumbPath = $thumbPath;
            return $thumbPath;
        }else{
            $this->error = '缩略图创建失败';
            @unlink($this->uploadFolder . '/' . $filename); // 删除上传的图片
            return FALSE;
        }
    } // end thumb

     // 说明：创建图片的存放目录
    private function _mkdir(){ // 创建图片上传目录和缩略图目录
        if(!is_dir($this->uploadFolder)){
            $mkdirResutlt = @mkdir($this->uploadFolder);
            if (!$mkdirResutlt){
                $this->error = '存放图片的目录创建失败';
                return FALSE;
            }
        }
        if(!is_dir($this->thumbFolder) && TRUE == $this->autoThumb){
            $mkdirResutlt = @mkdir($this->thumbFolder);
            if (!$mkdirResutlt){
                $this->error = '存放缩略图的目录创建失败';
                return FALSE;
            }
        }
    }
}


 // HTML中<input />的name属性值
	//echo($goods_img1);
	//echo str_replace("../","/",$goods_img1);
	//echo ($upfil2);
	//$upfil2= str_replace("upload","upload/thumb",(str_replace(".","_thumb.",(str_replace("../","/",$upfil2)))));//zhuanxiaotu
?>